# GOAL — Crush-Sim

## 목표 (2026-09-07 명시)

**셀 설계 초반에, 전문 해석 지식 없이도, 빠르고 쉽게 "개략적인 감"을 잡게
해주는 해석 파이프라인.** 정밀 해석을 대체하는 것이 아니라 그 앞에서 설계
방향을 거르는 도구다. 이것이 이 저장소의 전체적인 의의이며, 아래의 모든
기준은 이 목적에 종속된다.

| 예산 | 값 | 뜻 |
|---|---|---|
| 회전 시간 | **해석 1건 ≤ 30분**, 보통의 노트북(4코어급) 기준 | 넘으면 앞단 해석이 아니다. 메쉬·램프·종료 시간의 기본값은 이 예산 안에서 정한다 |
| 정확도 | **정밀 해석 대비 오차 ≤ 10%** | 그 이상 정밀할 필요 없다. 수렴 스터디는 "10% 안에 드는 가장 성긴 메쉬"를 찾기 위해 한다 |

대상: 셀 캔의 압착(LC-1~3)·벤팅(LC-6)부터 시작해 저항·열전파로 넓힌다
(`docs/HANDOVER.md` §1). 계산은 OpenRadioss explicit이 한다.

### 이 목표가 바꾸는 것
- **기본값은 성긴 쪽**이다. 예: 벤트 파단은 0.3 mm(엔진 7.3시간)가 아니라
  30분 안에 들면서 그 결과와 10% 안인 가장 성긴 메쉬가 기본이다. 0.5 mm는
  −5%로 정확도는 맞지만 엔진 39분이라 예산을 넘는다(2026-09-07 재확인) —
  예산 메쉬는 아직 찾는 중이다(`docs/LOG.md` §12).
- **게이트는 "정지"보다 "신뢰 등급 표시"에 가깝다.** 수치 게이트(에너지·질량)는
  유지하되, 앞단 사용자가 결과를 받지 못하는 상황을 최소화한다. 단 물리적으로
  틀린 모델(이중 벽, 미연결 조각)은 여전히 정지 — 틀린 감은 감이 아니다.
- **파라메트릭 형상이 1순위**, 실형상 STEP은 2순위. 설계 초반에는 치수가 아직
  안 정해져 있고, 치수를 바꿔 가며 돌리는 것이 곧 감을 잡는 과정이다.
- **결과는 숫자보다 경향**으로 보여준다(그래프·마일스톤·비교), 전문 용어는
  뷰어가 풀어서 쓴다.

## 성공 기준 (정량 — SPEC §7·§8·§12)
- 메시 게이트: SICN ≥ 0.3 · 종횡비 ≤ 5 · 최소 요소변 ≥ 0.3mm · 삼각형 ≤ 15%
- 해석 게이트: 에너지 오차 ≤ 5% · 아워글래스/내부E ≤ 10% · 운동E/내부E ≤ 5% · 부가질량 ≤ 2%
- 벤치마크: B-1 Alexander ±25% · B-2 링 강성 ±10% · B-3 메시 수렴 ≤ 5% · B-4 문헌 경향 일치
- `csim all --config <case.yaml>` 한 줄 재현, 동일 설정 재실행 시 결과 재현
- **회전 시간 ≤ 30분(4코어), 정밀 대비 오차 ≤ 10%** — 위 목표의 정량형

## 명시적 비목표
- 실시간 인터랙티브 시뮬레이션 (구 프로젝트 실패 원인 — ADR-02)
- 자체 물리 엔진 (ADR-01)
- 벤치마크 통과 전 UI (ADR-03). 벤치마크는 통과했고 UI는 SPEC §10(2026-09-07 개정: 비전문 설계자·30분·경향 중심)을 따른다

## 규율 (구 저장소에서 계승한 것은 프로세스뿐)
- EXP 단위 커밋. 실패한 실험은 reset하고 DEVLOG에 교훈만 남긴다.
- 메트릭은 게이트 통과율·벤치마크 오차. "보기 좋음"은 메트릭이 아니다.
