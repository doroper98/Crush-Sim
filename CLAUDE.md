# Crush-Sim — 작업 규칙

## 시작할 때 읽을 것

1. [`docs/HANDOVER.md`](docs/HANDOVER.md) — 프로젝트 개요, **쉘 이상화(두께가
   왜 화면에서 사라지는가)**, 이미 검증된 사실들
2. [`docs/LOG.md`](docs/LOG.md) — **시도했다 실패한 것들과 그 이유**
3. [`docs/SPEC-v2.1.md`](docs/SPEC-v2.1.md) — 요구사항·ADR (위반 시 리뷰 반려)

## 기록 규칙 — 이게 가장 중요합니다

**발견과 실패는 작업하면서 바로 기록합니다. 세션 끝에 몰아서 쓰지 않습니다.**

한 세션에서 알아낸 것을 대화에만 남겨두면 세션이 끝나는 순간 사라지고, 다음
세션은 같은 벽에 다시 부딪힙니다. 실제로 그런 사고가 있었습니다(`docs/LOG.md` §9).

무엇을 어디에 적는가:

| 종류 | 어디에 |
|---|---|
| **시도했다 실패한 것 + 왜 실패했나** | `docs/LOG.md` ← **이게 가장 자주 빠집니다** |
| 검증으로 확정한 사실 (부호 규약, 포맷, 상수) | `docs/HANDOVER.md` §4 + 해당 코드 주석 |
| 왜 이 값·이 방식인가 | 그 코드 바로 옆 주석 |
| 해석 결과 수치 | 해당 케이스 YAML 헤더 + `docs/VENT_BURST.md` |

기준: **"다음 사람이 이걸 모르면 같은 삽질을 반복하는가?"** → 그렇다면 적습니다.
결론만 적지 말고 **무엇을 시도했다 왜 실패했는지**를 함께 적습니다. 그게 없으면
다음 사람이 실패한 방법을 다시 시도합니다.

## 해석 실행

- **솔버는 직렬로만.** 4코어에서 엔진 2개를 돌리면 ~100배 느려집니다.
- **장시간 해석에는 예약 체크인을 겁니다.** 컨테이너 재시작이 실행 중인
  솔버와 bash 대기자를 죽입니다(예약 체크인은 살아남습니다).
- 결과 판정은 **`pipeline_summary.json`의 energy gate**로 합니다.
  엔진 콘솔의 에너지 오차는 마찰 접촉 에너지를 빼고 계산하므로 다릅니다.
- `runs/`는 git 제외입니다. 보존할 뷰어는 `viewers/`에 넣습니다.

## 코드

- **`ruff check`만** 씁니다. `ruff format`은 저장소 설정이 없어 무관한 파일을
  전부 재포맷합니다.
- 뷰어 템플릿 등 JS를 고쳤으면 **`node --check`로 문법 검사**합니다.
- 새 설정 필드에는 **검증을 같이 넣습니다**(`loading.tool`에 아무 문자열이나
  들어가던 적이 있습니다).
- 덱 생성(`crushsim/deck/writer.py`)을 고쳤으면 **고정 버전 스타터로 검증**하고
  골든 덱이 바이트 동일한지 확인합니다.
- 물리는 자체 구현하지 않습니다(ADR-01). 계산은 OpenRadioss가 합니다.

## 커밋

- 무엇을 왜 바꿨는지, **무엇을 시도했다 실패했는지**까지 본문에 적습니다.
- 커밋 메시지·PR·코드에 모델 식별자를 넣지 않습니다.
