# DEVLOG — Crush-Sim

실험 단위(EXP-###) 개발 기록. 형식: 날짜 · 목표 · 결과 · 교훈.
메트릭은 게이트 통과율·벤치마크 오차 (UI 점수 아님 — GOAL.md).

---

## EXP-001 (2026-08-22) — 저장소 스캐폴드 + Phase 0/1 골격

**목표**: SPEC v2.1 §11 구조로 신규 저장소 초기화. units.py 단일 정의,
FR-10 수확기 실행, 파라메트릭 지오메트리→메싱→게이트→덱 생성→CLI 골격,
pytest green, 어떤 머신에서든 clone→bootstrap→실행 가능한 재현 경로.

**결과**: 본 저장소 최초 커밋. 상세는 docs/EXECUTION_PLAN.md §5 (이번 PR의 범위).
- FR-10 수확기: 레거시 `MaterialModel.ts`에서 **재료 카드 243종** 생성, 전량 `verified: false`.
- pytest **248 passed, 2 skipped** (skip 2건은 솔버 결과가 필요한 B-1/B-2 대조 — 해석해 공식 자체는 19개 테스트로 상시 검증).
- Gmsh 파라메트릭 캔 1.2mm → 8,473 요소, 쿼드 100%, min SICN 0.391, 메시 게이트 통과. `Honda_Can.stp` STEP 메싱 동작.
- `csim all --skip-solver` 로 LC-1/LC-2 덱 생성 + UNVERIFIED 워터마크 HTML 리포트까지 관통. `csim doctor` 코어 전항 PASS(오프스크린 렌더 포함).
- 주의: 덱 키워드 문법은 실 starter 미검증 (writer·덱·리포트에 경고 명시, Phase 1.3 게이트).

**교훈(레거시 부검에서, 코드 반입 없이)**:
1. 물리는 검증된 솔버에 위임 — 안정화 계수는 재료 물성이 아니다.
2. 단위계는 한 파일에서만 정의한다.
3. 오프스크린 렌더 검증을 가장 먼저 한다 — 실패 시 이후 전부 헛수고.
4. 코어가 게이트를 통과하기 전에 표면(UI)을 다듬지 않는다.

**미결(다음 EXP)**:
- 솔버 설치 머신에서 OpenRadioss 공식 예제 1건 → MP4 관통 (Phase 0 완료 게이트)
- 덱 문법을 실 starter로 검증·수정 (Phase 1.3)

---

## EXP-002 (2026-08-22) — 실 솔버 검증: 덱 문법·엔진·후처리 전 구간 관통

**목표**: EXP-001의 덱 생성기를 실제 OpenRadioss(latest-20260728)로 검증하고
`csim all` LC-2를 끝까지 관통시킨다 (Phase 1.3~1.7 게이트).

**결과**: `csim all --config configs/cases/lc2_default.yaml` **전 과정 완주** —
starter 오류 0, 엔진 완주(9.1만 사이클), 공식 변환기(TH→CSV·ANIM→VTK),
MP4 3구도+GIF 렌더, UNVERIFIED 워터마크 HTML 리포트. 에너지 오차 1.2% (≤5% 통과),
아워글래스 0%, 부가질량 0%. 캔이 지그-V블록 사이에서 실제 압착됨
(전면 19.96mm / 배면 0mm), IE 22.6mJ ≈ 박판 링 압착 이론 스케일.

**수정 항목(전부 실 starter/엔진/포맷 정의로 검증)**: /SHELL 제목 제거 ·
/TH 객체 ID화 · LAW2/RBODY/TYPE7 누락 카드 · /PROP 필드 정렬 · /BEGIN 단위
20자 필드 · /ANIM TENS/STRESS 제거(엔진 세그폴트) · gmsh가 풀어놓는
RLIMIT_STACK 복원(자식 즉사 원인) · 절대경로화(install_root/RAD_CFG_PATH/변환기) ·
IMPDISP→IMPVEL(외부일 폭주) · Inacti=6 · 강체 두께 0.5mm+이격 · V블록 지지대
신설(캔 밀림 방지) · 접촉 인터페이스 3분할 + /TH/INTER(FN/FT) 반력 기록 ·
준정적 게이트를 변형체(CAN) KE 기준으로.

**교훈**:
1. cfg 포맷 정의(hm_cfg_files)와 QA 덱이 최고의 문법 레퍼런스다. 추측 금지.
2. 라이브러리(gmsh)가 부모 프로세스 상태(rlimit)를 바꿔 자식 솔버를 죽일 수 있다.
3. LC-2는 반대편 지지대 없이는 물리적으로 성립하지 않는다(캔이 밀려 도망감).
4. 구동 속도는 관성 지배 여부를 결정한다: 2 m/s에서 캔 KE/IE 52% → 0.5 m/s로 조정.

**미결(다음 EXP)**:
- **반력 채널 이상**: /TH/INTER FNX(≈0.019N)가 에너지 기반 평균 하중(≈1.1N)과
  ~60배 불일치. /TH/RBODY FX도 동일하게 작음. B-2(링 강성 해석해 ±10%)로
  힘 채널을 정량 교정할 것 — 접촉 강성(Istf/Stfac) 또는 TH 정의 재검토.
  → **EXP-003에서 해결**: 채널 자체가 힘이 아니라 누적 충격량이었다.
- 벤치마크 B-1/B-3 실판정, lc1_default 엔진 완주 확인. → B-1/B-2는 EXP-003.

## EXP-003 (2026-08-23) — §8 벤치마크 캠페인: B-1·B-2 실판정 PASS

**목표**: 스펙 §8 정량 벤치마크를 실제 솔버 결과로 판정한다 — B-1(Alexander
축방향 평균 압괴 하중 ±25%), B-2(탄성 링 횡강성, Roark 곡선보 이론 ±10%).

**결과 (판정은 `pytest -m bench`)**:
- **B-1 PASS**: 측정 248.6 N vs 기준 232.4 N → **+7.0%** (허용 ±25%).
  `configs/cases/bench_b1.yaml` — 평판 압축, 스트로크 30mm, 2 m/s.
  콘테이너 재시작으로 엔진이 t=0.01450/0.01579s(스트로크 92%, 27.4mm)에서
  중단됐으나 TH가 그 시점까지 플러시되어 평균 압괴 하중 판정에는 충분.
  사이클 로그 에너지 오차 −5.1%(경계), KE/IE 1.4%, 아워글래스 0, 부가질량 0.
- **B-2 PASS**: 측정 0.1464 N/mm vs 기준 0.1388 N/mm → **+5.5%** (허용 ±10%).
  `configs/cases/bench_b2.yaml` — 양면 평판(jig_plane+지지 평판), 스트로크 2mm.
  강성은 에너지식 k = 2·IE/δ²로 측정(감쇠 없는 접촉 링잉에 면역;
  힘-변위 기울기 fit은 탄성 진동 진폭이 정적 추세와 같은 자릿수라 불가).

**핵심 발견 — EXP-002의 "반력 채널 이상" 해결**:
OpenRadioss /TH의 운동학 개체(RBODY/INTER/RWALL) "힘" 채널은 힘이 아니라
**누적 충격량 ∫F·dt** 를 그대로 기록한다(엔진 소스 rgbodfp.F:
`FSAV += F*DT1`, thkin.F가 FSAV를 정규화 없이 기록). 물리적 힘은 시간
미분으로 복원 — `extract_force_displacement`가 자동 감지(`var` 서명 +
채널명)해 `np.gradient` + 이동평균(11점)으로 처리한다. 60배 불일치의 정체.

**수정 항목**:
- `post/curves.py`: 충격량 채널 자동 미분 · 접촉 시점 변위 재영점 ·
  **지배 성분 자동 선택**(`var NN` 그룹의 X/Y/Z 형제 채널 중 데이터 범위가
  가장 큰 축 선택 — 이름 매칭만으로는 항상 X를 집어 Z구동 케이스가 깨짐).
- `bench/test_benchmarks.py`: B-2를 에너지 강성식으로 판정, δ>0.5mm 가드.
- 신규 케이스 `bench_b1.yaml`/`bench_b2.yaml` (렌더 off, runs/bench_*).

**교훈**:
1. TH "힘" 채널의 단위를 믿지 말 것 — 엔진 소스가 최종 레퍼런스다.
2. 명시적 솔버의 탄성 미소변형 강성은 힘 fit이 아니라 에너지로 재라.
3. 축이 다른 케이스가 하나라도 생기면 이름 기반 채널 매칭은 즉시 깨진다.

**미결(다음 EXP)**:
- B-3 메쉬 수렴(1.2mm 기준 ±0.6/2.4mm 스윕) — 콘테이너 재시작이 잦아
  장시간 러닝은 로컬 PC에서 수행 권장.
- lc1_default(렌더 포함) 완주 확인, 실 지그 STEP(LC-2) 케이스.
- 엔진 사이클 에너지 오차가 축방향 압괴 후반 −5%에 근접(접촉 마찰 산일
  집계로 추정) — stop_on_energy_error 경계 재검토.

---

## EXP-004 (2026-08-31) — STEP 쉘 이상화: 임포트가 2겹 쉘을 만든다

> 기록 공백 주의: EXP-003(08-23) 이후의 작업(LC-4/5/6, UI, 뷰어, B-3)은
> DEVLOG가 아니라 `docs/HANDOVER.md`·`docs/VENT_BURST.md`에 기록돼 있다.
> program.md §1("DEVLOG에 목표를 먼저")이 그동안 지켜지지 않았다.

**목표**: 실형상 STEP 어셈블리(`can/test.stp` — CAN + 캡 + 벤트)로
can·cap·vent가 모두 보이는 벤트 파단 해석과 3D 뷰어를 만든다. 그 전제로
STEP 임포트의 쉘 이상화가 올바른지 먼저 판정한다.

**판정 기준 (GOAL.md 방식 — 정량)**:
1. 쉘 모델 질량 = `메시 면적 x 두께 x 밀도` 가 STEP 솔리드 질량
   (`체적 x 밀도`) 대비 **±10% 이내**. 쉘 이상화가 옳다면 중립면 면적과
   두께의 곱이 솔리드 체적과 같아야 한다.
2. 벽 단면에서 쉘이 **한 겹**일 것 (단면 절점 좌표로 확인).
3. §7 메시 게이트 PASS.

**착수 시점의 측정 — 현재 구현은 기준 1·2를 위반한다**:

`examples/step/Honda_Can.stp` (`lc2_step_example`, `lc2_step_broadside`,
`HondaCanViewer_lc2.html`, `honda_viewer.html`, `broadside_viewer.html`의
입력)를 `mesh_step_surfaces`로 메싱한 결과:

| | 값 |
|---|---|
| 메시 요소 | 15,711 |
| 중앙 높이 단면의 y 좌표 | **-6.28, -5.90, +5.90, +6.28** |
| 메싱된 면적 | 35,925 mm² (솔리드 전 표면적 36,129의 **99.4%**) |

y 좌표가 네 개 = 바깥벽·안쪽벽이 각각 메싱된 **2겹**이다. 간격 0.38mm가
곧 벽 두께. `mesh_step_surfaces`의 주석은 "the result is the outer skin as
shells (FR-02)"라고 하지만 `generate(2)`는 안팎을 모두 메싱한다 — 시트
바디였다면 맞는 가정이고, 두께 있는 솔리드에서는 틀리다.

정량 영향 (`aluminum_3003`, 케이스 `thickness: 0.3`):

| | 실제 | 2겹 모델 | 비 |
|---|---|---|---|
| 질량 | 20.3 g | 29.4 g | **1.45x** |
| 막 강성 | t=0.38 | 2x0.30=0.60 | **1.58x** |
| 굽힘 강성 | 0.38³ | 2x0.30³ | 1.01x |

굽힘이 맞는 것은 **우연**이다 — 두 쉘은 림에서만 붙어 있어 샌드위치로
거동하지 않고, `2x0.30³ = 0.054`가 `0.38³ = 0.0548`과 우연히 일치할 뿐이다.
`thickness`를 바꾸면 곧바로 어긋난다. 접촉면이 이중인 것도 별개 문제.

**왜 지금까지 안 잡혔나**: B-1·B-2·B-3 벤치마크가 **전부 파라메트릭 캔**
(`CanShell`/`BoxCan` — 처음부터 중립면 1겹)이다. STEP 경로에는 대조
벤치마크가 없어서 결함이 게이트를 통과한 채 살아남았다.

**바깥 껍질 판별 — 두 번 틀리고 세 번째에 맞았다**:

1. `BRepClass3d_SolidClassifier`로 면 법선 방향 점이 솔리드 내부인지 판정
   → **틀림**. 중공 캔의 공동은 솔리드가 아니다(솔리드는 벽뿐). 바깥
   껍질과 공동 라이닝이 **둘 다** "바깥"으로 나온다.
2. 면 방향(`TopAbs_REVERSED`)을 반영해 법선을 뒤집음 → **틀림**. 전부
   바깥으로 분류된다(Honda 47/0).
3. **레이 캐스팅** (`IntCurvesFace_ShapeIntersector`) → 맞음. 바깥
   방향으로 광선을 쏴서 솔리드를 **다시 만나면 공동 라이닝**, 안 만나면
   바깥 껍질. 공동은 닫혀 있으므로 반드시 반대편 벽을 만난다.

| 솔리드 | 바깥 껍질 | 공동 라이닝 |
|---|---|---|
| Honda 캔 | 27면 / 19,496 mm² | 20면 / 16,633 mm² |
| CAN_Body | 31면 / 133,674 | 20면 / 119,106 |
| Base_Plate (속 찬 판) | 59면 / 13,036 | 44면 / 596 |
| Vent (속 찬 판) | 25면 / 1,925 | 4면 / 0.3 |

바깥면만 메싱한 결과: **7,016요소(절반), 단면 y = ±6.28 한 겹, 게이트
PASS(min SICN 0.428)**.

**남은 것**: 속이 찬 판(캡·벤트)은 공동이 없어 경계 전체가 "바깥"이므로
여전히 2겹이 된다. 판에는 **지배면 하나 + t/2 오프셋**이라는 별도 규칙이
필요하다. 중공 박벽에는 바깥 껍질 + 안쪽 t/2 오프셋.


**판정 (Honda 캔, `crushsim/geometry/skin.py`)**:

| | 메싱 면적 | 쉘 체적 | 솔리드 7,447 대비 |
|---|---|---|---|
| 현재 구현 (2겹) | 35,925 | 13,652 | **+83.3%** |
| 바깥 껍질만 | 19,323 | 7,343 | **-1.40%** |
| + 중립면 오프셋 | 19,171 | 7,285 | -2.18% |

1. 질량 오차 **-1.40%** (기준 ±10%) **PASS**
2. 단면 y = -6.28, +6.28 — **한 겹** **PASS**
3. §7 메시 게이트 **PASS** (min SICN 0.399)

요소 수도 15,711 → 7,945로 절반이다.

**중립면 오프셋은 기본으로 켜지 않는다.** 바깥 껍질이 이미 -1.40%인데
오프셋을 넣으면 -2.18%로 **나빠진다** — 게이지 두께가 이 캔의 실제 평균보다
살짝 작아 지나쳐 간다. 오프셋은 옵션(`offset_to_mid_surface`)으로 두고,
기본 경로는 바깥 껍질을 쓴다. 곡률이 큰 부품에서는 다시 판단할 것.

**두께는 추정하지 말고 잴 것**: `체적/((바깥+공동)/2)`로 추정하면 Honda가
0.4122가 나오는데 실제는 0.38이다(단면 절점 간격으로 교차 확인). 공동
라이닝이 순수 오프셋보다 면이 적어 중립면을 과소평가하기 때문. 바깥면에서
안쪽으로 광선을 쏴 **직접 재면** 0.3800이 나온다. 단 평균은 못 쓴다 —
림·구멍 벽의 광선은 벽을 가로지르지 않고 부품을 길게 관통해서 평균을 두 배로
끌어올린다(0.76). **면적 가중 중앙값**을 쓴다. 결과는 전부 설계값처럼
떨어진다: Honda 0.3800, CAN_Body 0.6500, Base_Plate 2.4000.

**절점 오프셋은 면 방향을 먼저 정규화해야 한다**: 면 winding 그대로 절점
법선을 누적하면 봉합된 CAD처럼 winding이 섞인 곳에서 기여가 상쇄된다.
단위 테스트의 큐브 코너가 x·y는 안쪽(9.42)으로, z는 **바깥**(10.577)으로
갔다. 각 면의 Newell 법선을 부품 중심 기준으로 바깥 방향으로 뒤집은 뒤
누적하면 해결된다(별 모양 부품 가정 — 캔·판은 해당).

**교훈**:
1. **면적 기반 교차검증은 스스로를 검증하지 못한다.** 두께를 면적에서
   유도한 뒤 그 곱을 체적과 비교하면 항상 0%가 나온다(순환). 독립 측정이
   있어야 판정이다.
2. **벤치마크가 없는 경로에는 결함이 산다.** B-1~B-3이 전부 파라메트릭
   캔이라 STEP 경로의 2겹 결함이 살아남았다. 경로마다 정량 기준이 필요하다.
3. 이상치가 섞인 분포에서 평균은 쓰지 말 것 — 중앙값을 쓴다.

**영향 범위**: `lc2_step_example`, `lc2_step_broadside`, 그리고 그 결과인
`viewers/HondaCanViewer_lc2.html`, `honda_viewer.html`,
`broadside_viewer.html`, `BroadsideCrushViewer.html`. 질량 45%·막 강성 58%
과다 상태로 돌아간 결과다(굽힘은 `thickness: 0.3`과 우연히 상쇄).
**재실행 필요.**

**미결(다음)**: `mesh_step_surfaces`가 이 모듈을 쓰도록 배선 + `geometry.kind:
step`에 캡·벤트를 붙일 수 있게 하기(현재 `config.py:590`에서 벤트는
`box_can` 전용) → `can/test.stp` 실형상으로 can·cap·vent 벤트 해석 → 뷰어.

**어셈블리 용접 (`crushsim/meshing/assembly.py`)**:

실형상 `can/test.stp`(CAN 중공 + 캡 판 + 벤트 판)를 **한 모델에서** 메싱해
절점을 공유시킨다. 파트를 따로 메싱하면 절점 집합이 셋으로 갈려 덱 라이터의
`host`(호스트 절점 블록 재사용 = 용접)가 병합할 대상이 없다.

| | 절점 | CAN↔CAP | CAP↔VENT |
|---|---|---|---|
| 좌표 그대로 | 20,878 | 188 | **0** |
| `coplanar={'VENT':'CAP'}` | 21,157 | 188 | **156** |

**핵심 발견 — 판끼리는 각자 중립면으로 가면 만나지 않는다**: 캡의 지배면은
z=0(면적 5,845.9, 스타디움 구멍 이미 뚫림), 벤트의 지배면은 z=-2.0(면적
914.2 = 그 스타디움과 동일). 실물은 벤트가 캡 하부 링 턱(z=-1.6)에 용접되지만
**쉘 이상화가 그 턱을 지운다.** 두 판이 2mm 떨어져 뜬 채 아무것도 공유하지
않고, 벤트는 압력 아래 떠 있는 섬이 된다.

쉘 모델의 올바른 표현은 `box_can`이 처음부터 만드는 형태다 — **두 판을 같은
평면에 두고 포일이 구멍을 메우게** 하면 공유 외곽선이 곧 용접선이다. 두
면적이 정확히 일치하므로 벤트를 캡 평면으로 옮기면 맞는다.

결과: CAN t=0.650 / CAP t=2.400 / VENT t=0.300, 파트마다 다른 두께를 갖고
전 파트가 **동일 절점 배열**을 공유한다(덱 라이터 `host` 요구사항).

**미결**: `config.py:590`이 벤트를 `box_can` 전용으로 묶고 있어 아직
`kind: step` 케이스로 돌릴 수 없다. 스코어(실형상 S자 + obround 루프)의
요소별 두께 저감도 미적용.

**요소별 두께 — 파트당 하나의 숫자는 실부품에 대해 허구다**

두께 게이지의 면적 분포를 재보니 어느 파트도 균일하지 않다. `uniformity`는
중앙값 ±5% 안에 드는 면적 비율:

| 파트 | p10 | 중앙값 | p90 | uniformity |
|---|---|---|---|---|
| Honda 캔 | 0.380 | 0.380 | 0.700 | **74%** |
| CAN_Body | 0.650 | 0.650 | 0.944 | **79%** |
| Base_Plate | 2.400 | 2.400 | 5.640 | **55%** |
| Vent | **0.100** | 0.300 | 0.300 | **44%** |

벤트의 p10 = 0.100은 **스코어 잔여 두께**다 — 데이터에 이미 있는데 파트
중앙값(0.300)이 지워버린다. 경량화로 파낸 포켓, load path에 남긴 패드,
스코어는 전부 "의도적으로 공칭이 아닌" 곳이고, 파트당 한 값은 정확히
그것들을 지운다. 즉 해석이 확인하려던 설계 의도를 지운다.

**면당 한 샘플로는 못 잡는다**: 면 안에서 두께가 변하는 게 바로 그 경우라,
UV 중점 한 점이 어디에 떨어지느냐로 결정된다. 실제로 벤트 전체(1,709 요소)가
0.100mm로 뭉개졌다. `gauge_elements()`는 **요소마다** 광선을 쏜다(21k 요소에
12초).

**이상치를 반드시 거부해야 한다**: 벽을 못 만난 광선은 부품을 길게 관통해
218mm를 반환한다 — CAN 18,117 요소 중 **6,961개**. 클램프가 없으면 그대로
`/SHELL` 카드의 두께 필드로 들어간다. 중앙값의 3배 초과는 파트 대표값으로
대체(`ELEMENT_GAUGE_OUTLIER_FACTOR`).

**coplanar로 옮긴 파트는 원위치에서 재야 한다**: 벤트를 캡 평면으로 옮긴 뒤
그 좌표로 광선을 쏘면 원본 솔리드가 없는 허공을 재게 되어 전부 공칭값이
나온다. 이동량을 되돌려 원위치에서 잰다.

결과:

| 파트 | 이전(단일) | 요소별 실측 |
|---|---|---|
| CAN | 0.650 | 0.650 x14,157 · **1.200 x1,619** · **0.800 x1,243** |
| CAP | 2.400 | **2.000 x1,151** · 2.400 x237 · **1.700 x22** |
| VENT | 0.300 | **0.400 x6,337** · **0.100 x69 (스코어)** |

**용접을 게이트로**: 케이스가 용접이라 선언한 쌍의 공유 절점이 0이면
`MeshingError`. 숫자만 보고하던 상태는 §13.5(조용한 빈 결과 금지) 위반이었고,
실제로 첫 실행에서 `CAP↔VENT = 0`이었다 — 숫자를 안 봤으면 벤트가 떠 있는
채로 개변압이 나왔을 것이다.

**교훈**:
1. 파트당 하나의 물성은 파라메트릭 형상의 습관이다. 실 CAD는 파트 안에서
   변하고, 변하는 그 지점이 대개 해석의 대상이다.
2. 샘플 하나로 면을 대표시키지 말 것. 대표시키려면 그 면이 균일하다는
   증거가 있어야 하고, 그 증거가 `uniformity`다.
3. 기하 질의는 실패를 값으로 반환한다(218mm). 물리적으로 불가능한 값을
   거르지 않으면 조용히 덱까지 간다.

**임의 형상 견고성 판정 — 세 방어는 근본 해결책이 아니었다**

튜닝하지 않은 저장소의 다른 STEP(`pris_can`, `cylin_can`, `side_terminal`,
`Honda_Cap_Assy`)으로 확인하니 곧바로 깨졌다:

- `HOLLOW_AREA_SHARE_MAX = 0.85`의 독스트링에 "두 집단 간격이 넓다"고 썼는데
  **4개 샘플 기준이고 거짓이다.** 넓은 표본에서 share는 0.79~0.95에 연속
  분포하고 **0.839 → hollow, 0.861 → plate** 로 0.017 차이가 규칙을 바꾼다.
- `uniformity 0%`, `t = 0.7071`(대각선 아티팩트), 282면 중 216면 폐기 같은
  결과가 나온다.
- `side_terminal` solid 1은 두께 **17.565mm**로 잡힌다 — 이상치 필터가
  파트 중앙값 기준이라, 중앙값 자체가 틀리면 무력하다.

세 방어 중 **진짜 게이트는 용접 하나**였고 그것도 "공유 절점 > 0"이라
코너 한 점 접촉이 통과했다. 나머지 둘은 자기참조이거나 방법론적 개선이다.

**질량 보존이 유일하게 판별력 있는 지표다**

| 부품 | 질량 오차 | 판정 |
|---|---|---|
| 캔 4종(중공) | -6.6 ~ -9.1% | PASS |
| side_terminal 5,6,13,14 | -0.9 ~ +1.0% | PASS |
| side_terminal 1,9 (터미널 블록 t17.6) | **+683 ~ +692%** | FAIL |
| 판 8종 | ±11 ~ 33% | FAIL |

`uniformity`는 판별력이 없다 — **통과 부품이 43%, 실패 부품이 90%**다.
두께가 변한다는 사실은 품질이 아니라 설계 의도이고(포켓·패드·스코어),
그건 요소별 두께가 처리한다. 그래서 `info`로 보고만 하고 **게이트에서 뺐다.**
여기에 게이트를 걸면 이 파이프라인의 존재 이유인 부품들을 거부하게 된다.

`+683%`짜리는 **애초에 쉘로 만들면 안 되는 부품**이다(두께 17.6mm 터미널
블록). 질량 오차가 정확히 그것을 말한다: "이 솔리드는 쉘이 아니다."

**요소별 두께가 질량을 맞춘다 — 이게 게이트 통과의 조건이다**

| 파트 | 단일 두께 | 요소별 두께 |
|---|---|---|
| CAN | +1.39% PASS | +4.40% PASS |
| CAP | **+11.22% FAIL** | **+1.98% PASS** |
| VENT | **+22.66% FAIL** | **+0.17% PASS** |

벤트가 +22.7% → +0.17%. 스코어를 반영하니 질량이 맞는다. 즉 요소별 두께는
편의 기능이 아니라 **이 부품이 쉘로 성립하기 위한 조건**이다.

**용접은 접합선 길이로 판정한다**: 공유 절점 수는 코너 한 점 접촉을 걸러내지
못한다. 파트 자유경계 중 공유된 길이의 비율(`SHELL_WELD_SEAM_FRACTION_MIN`)로
바꿨다. 측정: CAP 81.6%, VENT 100%.

**게이트는 사람이 형상을 보는 단계가 아니다.** GOAL.md의 "성공 기준은 시각적
재현이 아니라 게이트 통과율"이 그대로 적용된다 — 숫자 vs 한계 → PASS/FAIL →
실패면 정지(ADR-06). 눈으로 보는 것은 `csim preview`와 뷰어이고 그건 이해를
돕는 것이지 판정이 아니다. 둘을 섞으면 매번 사람이 봐야 하는 시스템이 된다.

**남은 취약점(정직하게)**: 솔리드 CAD의 중립면 자동 추출은 미해결 문제이고
상용 전처리기도 반자동이다. `plate = 지배면 하나`는 성형된(평평하지 않은)
판에서 무의미하고, `coplanar = Z축 강체 이동`은 적층 방향이 Z가 아니면
무의미하다. 지금 상태는 **게이트가 실패를 조용히 넘기지 않는다**는 것이지
**임의 형상이 자동으로 된다**는 것이 아니다. 중립면 시트바디를 입력으로
받는 경로가 근본 해법에 가깝다.
